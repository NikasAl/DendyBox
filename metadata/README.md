# Метаданные для RuStore

Тексты карточки приложения для публикации в RuStore. Папка на каждую игру —
свои тексты (как и сборка: одна игра — одно приложение `com.dendybox.app.<flavor>`).

## Структура

```
metadata/
  robocop3/
    title.txt               — название приложения (лимит 30 символов)
    short_description.txt   — краткое описание (лимит 80 символов)
    full_description.txt    — полное описание (лимит 4000 символов)
    release_notes.txt       — «что нового» для первой версии
    category.txt            — рекомендуемая категория (справочно)
    age_rating.txt          — рекомендуемый возрастной рейтинг (справочно)
    tags.txt                — теги/ключевые слова (справочно)
    screenshots/            — сюда положить снятые скриншоты
                              (./scripts/screenshots.sh <flavor>, 3–10 шт.,
                              лучшие — в начало; первый решает, установят ли)
    icon.png                — исходная иконка игры (кладёте вы)
    icon512.png             — иконка 512×512 для карточки RuStore
                              (генерируется из icon.png скриптом, коммитим)
    background.png          — постер меню сборника «X игр в 1» (кладёте вы,
                              используется только у flavor-сборников; в APK
                              автоматически пережимается в WebP)
```

## Иконка игры

Положите исходник в `metadata/<flavor>/icon.png` (лучше квадрат 512×512 или больше,
PNG, допустима прозрачность). Дальше всё автоматически:

* `./scripts/make_icons.sh <flavor>` — или просто сборка `build_release.sh`:
  из icon.png готовится adaptive-иконка лаунчера (подхватывается APK) и
  `icon512.png` для карточки магазина;
* если у исходника прозрачные углы («иконка с рамкой») — арт ставится целиком
  на подложку среднего цвета арта; если квадратный непрозрачный — арт заливает
  весь холст, маска лаунчера сама режет края;
* обновили icon.png — просто пересоберите; иконка перегенерируется сама.

## Фон меню сборника

Только для flavor-сборников («X игр в 1», см. roms/README.md): положите
постер в `metadata/<flavor>/background.png` (или `.jpg`, `.webp`) — сборка
автоматически пережмёт его в WebP (длинная сторона ≤ 2048px; файл меньше
256 КиБ вставит как есть) и встроит в APK, а экран выбора игры покажет
картинку на весь экран за списком игр. Постера нет — меню на чёрном фоне
(тоже рабочий вариант).

Рекомендации к картинке:

* **горизонтальный** формат: приложение работает в альбомной ориентации
  (например 1920×1080 или шире; приложение само отмасштабирует с
  кадрированием под конкретный экран);
* главный сюжет — выше и ниже середины: там шапка и подсказки; под список
  игр оставляйте полосу по центру (или сместите композицию к краю и задайте
  `"menuAlign": "left"`/`"right"` в roms/games.json);
* сверху и снизу приложение добавляет мягкое затемнение (шапка и подсказки
  читаются на любом арте), это можно учитывать при композиции;
* исходник можно не ужимать: пересжатие в WebP произойдёт при сборке,
  оригинал в `metadata/` останется нетронутым.

## Чек-лист публикации (RuStore)

1. Подписанный релиз: `./scripts/build_release.sh <flavor>`
2. Скриншоты: `./scripts/screenshots.sh <flavor> 15 20` — играть во время съёмки;
   для карточки брать `shot_NN_16x9.png` (16:9 — требования RuStore по
   соотношению сторон не выше 2:1), родные снимки — для соцсетей.
   Видео геймплея (по желанию): `sudo pacman -S scrcpy` и
   `./scripts/screencast.sh <flavor> 90` — со звуком игры (Android 11+)
3. Иконка: `metadata/<flavor>/icon512.png` (генерируется из icon.png автоматически —
   см. «Иконка игры» выше; при сборке она и так обновится)
4. Заполнить карточку текстами из файлов выше (скопировать как есть);
   проверить лимиты: `python3 scripts/check_metadata_limits.py <flavor>`
5. Возрастной рейтинг — по содержимому (см. age_rating.txt), категория — category.txt
6. Загрузить APK, приложить скриншоты, отправить на модерацию

## Тон текстов

* Аппелируем к **Dendy** и ностальгии по 90-м в России — приставка, джойстик,
  «картридж», телевизор.
* Дружелюбно, по-человечески, без канцелярита; восторг — сдержанный.
* Честно перечисляем функции (см. README.md проекта): редактор раскладки,
  турбо-кнопки, квик-сейвы и 10 слотов, офлайн, без рекламы.
